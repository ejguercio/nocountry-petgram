export const ErrorMessage = ({ message }) => {
  return (
    <>
      <span className="h-[0px] mb-[20px] mt-[-5px] flex justify-center text-[#ea3354]">
        {message}
      </span>
    </>
  );
};
